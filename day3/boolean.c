// import the standard boolean library
#include <stdbool.h>
#include <stdio.h>
int main (){
    // declare a boolean variable
    bool isRaining = true;
    bool isSunny = false;

    // print the value of the boolean variables
    printf("Is it raining? %d\n", isRaining); // prints 1, which means true
    printf("Is it sunny? %d\n", isSunny); // prints 0, which means false

    return 0;
}
